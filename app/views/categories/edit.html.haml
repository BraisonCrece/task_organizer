.category.edit-page
  .card
    .card-body
      %h2 Editando categoría
      = render 'form'
      
      = link_to t('common.show'), @category, class: 'btn btn-dark'
      
      \|
      
      = link_to t('common.back'), categories_path, class: 'btn btn-dark'