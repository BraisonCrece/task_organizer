.category.show-page
  .card
    .card-body
      %p
        %b Nombre:
        = @category.name
      %p
        %b Descripción:
        = @category.description
      = link_to t('common.edit'), edit_category_path(@category), class: 'btn btn-dark'
      \|
      = link_to t('common.back'), categories_path, class: 'btn btn-dark'